# Positive project-control runtime corpus

This directory contains valid project-owned control-plane records derived from behavior implemented in the repository. These records are maintained separately from the positive scientific corpus.

The records exercise the generic engine boundary against its own implemented discovery and lifecycle behavior.

The `engine-lifecycle/` and `engine-lifecycle-profile-0.1.0/` packages declare the same implemented discovery and inspection behavior against exact Profile 0.2.0 and 0.1.0 contracts. Their explicit comparison is the positive plugin-package migration case.
